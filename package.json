{
  "name": "finspectre-backend",
  "version": "1.0.0",
  "description": "FinShield — Intelligent Banking Financial-Resilience Platform & Backend Test Suite",
  "main": "scripts/test-runner.js",
  "scripts": {
    "test": "node scripts/test-runner.js",
    "test:all": "node scripts/test-runner.js",
    "test:status": "node scripts/test-runner.js --status",
    "test:health": "node scripts/test-runner.js --layer=health",
    "test:models": "node scripts/test-runner.js --layer=models",
    "test:engines": "node scripts/test-runner.js --layer=engines",
    "test:api": "node scripts/test-runner.js --layer=api",
    "test:verbose": "node scripts/test-runner.js -v",
    "test:pytest": "python -m pytest backend/tests -v",
    "test:coverage": "python -m pytest backend/tests --cov=backend/app --cov-report=term-missing",
    "backend:start": "python -m uvicorn backend.app.main:app --reload --host 0.0.0.0 --port 8000",
    "backend:seed": "python -c \"from backend.app.models.database import SessionLocal; from backend.app.seed.seeder import seed_database; seed_database(SessionLocal())\"",
    "backend:health-check": "node -e \"const http=require('http'); http.get('http://localhost:8000/health', (r) => { let d=''; r.on('data', c=>d+=c); r.on('end', () => console.log(JSON.stringify(JSON.parse(d), null, 2))); }).on('error', (e) => console.error('Backend offline or unreachable on port 8000:', e.message));\""
  },
  "keywords": [
    "finshield",
    "finspectre",
    "fastapi",
    "pytest",
    "resilience-score",
    "banking",
    "debtkart"
  ],
  "author": "FinShield Engineering Team",
  "license": "MIT"
}
